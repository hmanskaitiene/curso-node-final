       <div class="row">
            <div class="card text-bg-light">
                <div class="card-body  d-flex">
                    <div class="p-2 align-self-center">
                        <a href="/productos" class="btn btn-outline-primary">Inicio</a>
                    </div>
                    <div class="p-2 flex-grow-1 align-self-center">
                        <h5 class="d-none d-md-block">Hola {{userLogged.nombre}}!</h5>
                    </div>
                    <div class="p-2 align-self-center">
                        <a href="/carrito" class="btn btn-primary">
                        <i class="bi bi-cart"></i>
                        <span class="badge text-bg-secondary" id="badgeCantProductos"></span>
                        </a>
                    </div>
                    {{#if (eq userLogged.rol 'ADMIN') }}
                    <div class="p-2 align-self-center">
                        <a href="/chat" class="btn btn-primary">
                        <i class="bi bi-chat"></i>
                        </a>
                    </div>
                    <div class="p-2 align-self-center">
                        <a href="/stock" class="btn btn-primary">
                            <i class="bi bi-shop"></i>
                        </a>
                    </div>
                    {{else}}
                    <div class="p-2 align-self-center">
                        <a href="/chat/{{userLogged.email}}" class="btn btn-primary">
                        <i class="bi bi-chat"></i>
                        </a>
                    </div>
                    {{/if}}                
                    <div class="p-2">
                        <div class="dropdown">
                        <button class="btn btn-light dropdown-toggle" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
                            <img class="rounded-circle article-img" src="{{userLogged.imageProfile}}" id="menuImageDisplay" style="width: 40px; height: auto;">
                        </button>
                        <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
                            <li><a class="dropdown-item" href="/profile">Mis datos</a></li>
                            <li><a class="dropdown-item" href="/orden">Mis ordenes</a></li>
                            {{#if (eq userLogged.rol 'ADMIN') }}
                            <li><a class="dropdown-item" href="/info">Información</a></li>
                            {{/if}} 
                            <li><a class="dropdown-item" href="/logout">Salir</a></li>
                        </ul>
                        </div>
                    </div>

                </div>
            </div>
        </div>
